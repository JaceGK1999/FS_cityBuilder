export function createStatsString(climateNum, areaNum, homeNum) {
    return `You have changed the climate type ${climateNum} times, the home type ${homeNum} times, and the area ${areaNum} times. And nobody can forget your city's classic catchphrases:`;
}
